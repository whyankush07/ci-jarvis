version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: devagent-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-devagent}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-devagent}
      POSTGRES_DB: ${POSTGRES_DB:-devagent}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-devagent} -d ${POSTGRES_DB:-devagent}" ]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: devagent-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: [ "CMD", "redis-cli", "ping" ]
      interval: 5s
      timeout: 5s
      retries: 5
  # server:
  #   build:
  #     context: ..
  #     dockerfile: docker/Dockerfile.server
  #   container_name: devagent-server
  #   restart: unless-stopped
  #   env_file:
  #     - ../.env
  #   ports:
  #     - "8080:8080"
  #   depends_on:
  #     postgres:
  #       condition: service_healthy
  #     redis:
  #       condition: service_healthy
  #   volumes:
  #     - ..:/app:ro
  #   healthcheck:
  #     test: ["CMD-SHELL", "curl -f http://localhost:8080/ || exit 1"]
  #     interval: 10s
  #     timeout: 5s
  #     retries: 5

volumes:
  postgres_data:
  redis_data:

